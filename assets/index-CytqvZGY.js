function getDummySession() {
  return { sid: "dummy", timestamp: 0, errors: 0, status: "ok" };
}
      (e._session = getDummySession()),
      (this._session = getDummySession()),
    return this._session || getDummySession();
    s = e.getSession(),
    i = getDummySession();
  (n && ste(n), Q7(), t.setSession(getDummySession()));
