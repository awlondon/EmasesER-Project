  mce = et.object({ session_id: et.string().default("") }),
      ""
              session_id: "",
        sessionId: "dummy",
          session_id: "dummy",
      sessionId: "dummy",
      sessionId: _,
      sessionId: "dummy",
  return [t.type, t.eventId, "dummy", t.tenantId]
    await this.transport.connect();
    this.sessionId = "";
    this.pingIntervalHandle = setInterval(() => {
      this.ping();
    }, 1e3);
    return this.sessionId;
      session_id: "",
    ((this.sessionId = ""), (this.transport = n));
      session_id: "",
    await E.connect();
    const S = "";
      i.processSessionStarted(S, p),
      this.logManager.setContext("session_id", ""),
                sessionID: "dummy",
            data: { sessionID: "dummy", startTime: C, lastUpdate: C },
            ((C.sessionID = "dummy"), (C.startTime = R)),
            { stableID: J, sessionID: "dummy", sdkType: ie, fallbackUrl: W },
            session_id: "dummy",
            session: { id: "dummy" },
      if (n) return { session_id: "dummy" };
