// appCore.js — stubbed for development
export function initApp(root) {
  root.innerHTML = `
    <main style="padding: 2rem; font-family: sans-serif;">
      <h1>🟢 Emases Local Build Active</h1>
      <p>Interface initialized via <code>initApp()</code>.</p>
    </main>
  `;
}
